// src/constants/healthColors.ts
// Health severity intensity scale: 1=minimal (green) → 5=critical (dark red)
// Used by IntensityPicker (UI.tsx) and EventCard intensity badges.
export const INTENSITY_COLORS = ['#1DD1A1', '#FECA57', '#FF9F43', '#FF6B6B', '#CC2222'] as const;
